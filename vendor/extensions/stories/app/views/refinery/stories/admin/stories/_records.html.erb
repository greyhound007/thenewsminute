<%= render 'refinery/admin/search_header', :url => refinery.stories_admin_stories_path %>
<div class='pagination_container'>
  <% if @stories.any? %>
    <%= render 'stories' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
