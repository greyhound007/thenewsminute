<%= render 'refinery/admin/search_header', :url => refinery.landing_pages_admin_landing_pages_path %>
<div class='pagination_container'>
  <% if @landing_pages.any? %>
    <%= render 'landing_pages' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
