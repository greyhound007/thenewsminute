<%= render 'refinery/admin/search_header', :url => refinery.specials_admin_specials_path %>
<div class='pagination_container'>
  <% if @specials.any? %>
    <%= render 'specials' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
