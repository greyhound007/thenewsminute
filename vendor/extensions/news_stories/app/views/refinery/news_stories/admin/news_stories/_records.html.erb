<%= render 'refinery/admin/search_header', :url => refinery.news_stories_admin_news_stories_path %>
<div class='pagination_container'>
  <% if @news_stories.any? %>
    <%= render 'news_stories' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
