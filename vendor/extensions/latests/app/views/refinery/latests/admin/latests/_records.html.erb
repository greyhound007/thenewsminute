<%= render 'refinery/admin/search_header', :url => refinery.latests_admin_latests_path %>
<div class='pagination_container'>
  <% if @latests.any? %>
    <%= render 'latests' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
