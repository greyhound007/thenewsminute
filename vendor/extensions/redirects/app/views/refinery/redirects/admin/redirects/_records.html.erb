<%= render 'refinery/admin/search_header', :url => refinery.redirects_admin_redirects_path %>
<div class='pagination_container'>
  <% if @redirects.any? %>
    <%= render 'redirects' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
