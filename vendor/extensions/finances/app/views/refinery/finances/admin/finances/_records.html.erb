<%= render 'refinery/admin/search_header', :url => refinery.finances_admin_finances_path %>
<div class='pagination_container'>
  <% if @finances.any? %>
    <%= render 'finances' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
