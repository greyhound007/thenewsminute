<%= render 'refinery/admin/search_header', :url => refinery.side_headlines_admin_side_headlines_path %>
<div class='pagination_container'>
  <% if @side_headlines.any? %>
    <%= render 'side_headlines' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
