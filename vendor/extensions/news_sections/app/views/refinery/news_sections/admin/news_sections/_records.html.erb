<%= render 'refinery/admin/search_header', :url => refinery.news_sections_admin_news_sections_path %>
<div class='pagination_container'>
  <% if @news_sections.any? %>
    <%= render 'news_sections' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
