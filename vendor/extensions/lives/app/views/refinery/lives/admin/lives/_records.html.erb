<%= render 'refinery/admin/search_header', :url => refinery.lives_admin_lives_path %>
<div class='pagination_container'>
  <% if @lives.any? %>
    <%= render 'lives' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
