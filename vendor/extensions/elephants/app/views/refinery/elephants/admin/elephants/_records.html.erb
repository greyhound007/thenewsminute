<%= render 'refinery/admin/search_header', :url => refinery.elephants_admin_elephants_path %>
<div class='pagination_container'>
  <% if @elephants.any? %>
    <%= render 'elephants' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
