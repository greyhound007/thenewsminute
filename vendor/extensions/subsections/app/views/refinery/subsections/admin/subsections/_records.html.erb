<%= render 'refinery/admin/search_header', :url => refinery.subsections_admin_subsections_path %>
<div class='pagination_container'>
  <% if @subsections.any? %>
    <%= render 'subsections' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
