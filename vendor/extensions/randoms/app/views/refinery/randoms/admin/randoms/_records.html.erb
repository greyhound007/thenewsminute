<%= render 'refinery/admin/search_header', :url => refinery.randoms_admin_randoms_path %>
<div class='pagination_container'>
  <% if @randoms.any? %>
    <%= render 'randoms' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
