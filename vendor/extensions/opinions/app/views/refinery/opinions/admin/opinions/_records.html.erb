<%= render 'refinery/admin/search_header', :url => refinery.opinions_admin_opinions_path %>
<div class='pagination_container'>
  <% if @opinions.any? %>
    <%= render 'opinions' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
